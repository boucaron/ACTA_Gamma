[project]
name = "acta-gamma"
version = "0.1.0"
description = "ACTA Gamma UI prototype"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
