# Current status

Early prototype / POC — not a product. By design (see `docs/DBDesign.md`, "Scope and assumptions"): no schema versioning or migration framework, no purge/hard-delete of rows (create a new DB file if a clean state is needed), and `acta_db` is not thread-safe — each consumer (CLI, runner, GUI) owns its own single connection, and simultaneous writers on one file are out of scope.

**Done:**

- Entity model and persistence: C library + CLI + GUI over SQLite.
- Skill/model versioning and folder organization.
- Execution lifecycle and execution log (`execution_log` phase rows).
- Replayable immutable contexts. `context create` derives the content hash when omitted: SHA-256 of the content, lowercase hex — the same rule the GUI applies (SHA-256 implementation from Brad Conte's crypto-algorithms, public domain; see `acta_cli/src/sha256.c` and the README "Third-party code" section). An explicitly supplied `--hash` / JSON `hash` is stored as-is.
- Standalone runner (`acta_runner`): claim → resolve → preflight → OpenAI-compatible chat call → raw response capture → optional output-schema validation → complete/fail (see `docs/runner_contract.md`).
- In-app "Run" button: the GUI runs the runner's pipeline in-process on a worker thread, directly using the same runner source files (`acta_runner/src/run.c`, `acta_runner/src/backend.c`) — one shared pipeline codebase, no duplicated pipeline logic — on its own DB connection, with live status polling of the shared database. While a run is in flight the button toggles into Cancel, which cooperatively cancels the run and transitions the row to `cancelled`.
- `sweep` (`acta_runner sweep --stale-seconds N`) cleans up executions left in `running` after a dead runner process: an execution is stale when its last runner activity — the latest of its newest `execution_log.created_at` and `started_at` (falling back to `created_at`) — is older than `now − N` seconds. A stale row transitions `running → failed` with the error `stale running: no runner activity for N s` and an `execution_failed` log row; any row that leaves `running` between the query and the fail is skipped rather than overwriting a live outcome. `--stale-seconds` is required (there is no default) and must be a positive integer (0 is rejected).
- Normal timeout handling is done by the runner itself (hard per-call HTTP timeout, `--timeout`, default 600 s), and rerun of failed executions (`failed → pending` via `acta_db_execution_reset`, exposed by the GUI Retry button and by `acta_cli exec reset <id>`).
- Soft-delete lifecycle for contexts and executions: complete across `acta_db` (`delete`/`restore` flag flips, live-only lister defaults, `_with_deleted`/`include_deleted` variants, `create`/`reset` refusal of deleted rows, `acta_runner` claim guard), `acta_cli` (`delete`/`restore`, `--include_deleted`/`--deleted`, `deleted_at` in output) and `acta_gui` (trash views with per-row Delete/Restore in the context and execution panels) — design rules and the one-time DB migration in `docs/DBDesign.md`, CLI surface in `docs/cli_spec.md`, GUI smoke-verified via `acta_gui/db/smoke_test.sh`.

- Space-free app-data directory and config filename (review papercut): the default app-data directory is renamed `ACTA Gamma` → `ACTA_Gamma` and the per-machine config file `ACTA Gamma.conf` → `ACTA_Gamma.conf`. All path builders updated in lockstep — `acta_conf_default_path()` (`acta_db/src/conf.c`), both `acta_dbpath.c` copies (CLI + runner), `MainWindow::defaultDbPath` (GUI), and the `acta_cli --db` help text (`src/commands.c`) — with test expectations updated (`acta_cli/tests/conf`, `acta_cli/tests/dbpath`, `acta_runner/tests/run/test_conf.c`) and docs updated (README, `docs/cli_spec.md`, `docs/known_issues.md`, `docs/building.md`, `docs/runner_contract.md`). One-time migration for existing machines: their `acta.db` / config in the old space-bearing `ACTA Gamma` directory are no longer found by default — move (or recreate) them under `ACTA_Gamma`.
- Unified GUI/CLI/runner default DB path (code-review item "The dual-default DB path is a footgun"): all three binaries now default to the same org-free app-data file — `%APPDATA%\ACTA_Gamma\acta.db` (Windows) / `~/.local/share/ACTA_Gamma/acta.db` (POSIX) — with `./acta.db` as a last-resort fallback and a hint naming a legacy `./acta.db` when the default DB is missing. Resolution lives in `acta_dbpath.c` (duplicated in lockstep in `acta_cli/src` and `acta_runner/src`) and is mirrored in `MainWindow::defaultDbPath`; pinned by the new `acta_cli/tests/dbpath` suite; the `--db` contract in `docs/cli_spec.md` is the single source of truth. QSettings org name changed `boucaron` → `ACTA Gamma` (previously remembered GUI settings reset once; see `docs/known_issues.md` #12).
- Light-projection listers for the high-volume entities (resolves known issue 1): `acta_db_context_query_light` / `_with_deleted_light` and `acta_db_execution_query_light` omit the blob columns (`content`; `raw_response` / `result` / `error`) — blob fields are `NULL` in returned rows, struct shapes and pagination unchanged; covered by `acta_db/tests/test_light_queries.c`. Adopted by: `acta_cli` `context list` / `exec list` (light by default, `--full` for blobs), the GUI (`ContextPanel::reload`, `ExecutionPanel::reload`, `ExecutionCreateDialog::loadParentExecutions`), while single-row getters (`context get`, `exec get`, `acta_db_execution_get` polls) and the create-dialog context tooltip keep the full blobs. UI trade-off: the context panel filter matches Type + Date (no content search, since the blob is no longer materialized in the list).
- Open-time pragma check-and-report (resolves known issue 2): `acta_db_open` captures the `PRAGMA journal_mode=WAL` / `PRAGMA foreign_keys=ON` results and queries the effective `PRAGMA journal_mode;` (the set-pragma can return OK while silently staying non-WAL, e.g. on `:memory:`). On degradation it stores an informational note in `last_error` — the open still returns `ACTA_DB_OK` and the connection is fully usable; the note is cleared by the next `acta_db_exec*` call. Documented in the `acta_db_open` header comment and `docs/DBDesign.md` (Scope and assumptions); the GUI (`DbHandle::open`), CLI, and runner warn on the note after a successful open. Covered by `test_db_open_file_wal_active` and `test_db_open_memory_wal_note` in `acta_db/tests/test_db.c`.
- `acta_db_exec` contract documented (resolves known issue 3): the `acta_db_exec` header comment now states that `sql` must be static/developer-supplied (DDL, migrations, schema scripts), never composed from user-supplied input, and lists the intended callers (GUI first-launch `schema.sql`, CLI `db exec`); the constraint is also in `docs/DBDesign.md` (Scope and assumptions).
- Per-action help in `acta_cli`: `X help <action>` and `X <action> --help` print a single action's section for every entity; bare `X help` / `X --help` / `--help` keep printing the full entity help / global usage. Both entry points funnel into the same per-entity action→section lookups (`X_help_for_action`), so they cannot drift; `db` and `context` help were refactored into header + section functions + footer (single source of truth), and `parse_globals` no longer short-circuits on `--help` (it collects the entity/action tokens for routing; unknown entity or action with `--help` is the canonical exit-10 JSON error). Covered by the new `acta_cli/tests/help` suite and the `tests/gparse` help-routing seam tests.
- `model_revision` soft-delete surface completed (`docs/known_issues.md` #2–#4, commits `d678a12`, `4451244`, `c437b3e`): `get` returns not-found (rc 1) for a soft-deleted revision, `get-latest` returns the latest *live* revision, and `list` / `count` default to live-only rows with `--include_deleted` / `--deleted` to opt back in (new `acta_db_model_revision_list_by_model_with_deleted` / `acta_db_model_revision_count_with_deleted` variants, flag exposed on the `--tools` schema and in `docs/cli_spec.md`). Pinned by the `model_revision_test_deleted` CLI suite and the `test_mr_get_deleted` / `test_mr_get_latest_skips_deleted` / `test_mr_list/count_excludes_deleted` / `*_with_deleted_includes_deleted` DB tests; stale deleted-row assertions in `test_integration.c` and `test_model.c` corrected.
- Structured `success` in `acta_cli --tools` (commit `1bb0466`): per entry, `success` is now a JSON object `{"kind":"json"|"json_object"|"json_array"|"bare_int"|"plain_text"}` — kind `"json"` carries the exact wire `"keys"`, an optional `"note"` carries the `--table` / `--count` variants — replacing the prose string; the schema `version` is bumped 1 → 2 in both the full JSON and the `--tools --compact` header. Implementation in `src/tools.c` (`tool_success_t` + `suc_*` constants + `jf_success` renderer); `tests/tools` expects version 2 and checks the per-entry `success` shape; the wire-format contract paragraph is in `docs/cli_spec.md`. Verified in the rebuilt binary (`test_tools` passes).
- Help small fixes in `acta_cli`: the `--db` line in `help_print` (`src/commands.c`) documents the default DB path (`$ACTA_DB`, else the app-data file shared with the GUI, `./acta.db` only as a last resort), and `tools_print_compact` (`src/tools.c`) only emits the ` / ` required/optional JSON separator when the required list is non-empty, so `skill.update` renders `json:name,prompt_template,folder_id,description,output_schema` instead of `json: / name,…`. No schema or test-shape change.
- `api_key` removed from the model `configuration` blob (`4c3fe27` and predecessors) and the `--api_key` CLI flag removed (`97fe915`, `0ce3826`): the key is never stored in the database — auth is `$OPENAI_API_KEY` or, when that variable is unset, the `"api_key"` key of the per-machine config file (`ACTA_Gamma.conf` — see the Done note below): key present in neither source → the run does not start (runner `ACTA_RUNNER_ERROR` exit 4 before any claim, GUI error in the run result); empty key (from either source) → warning and no `Authorization` header — and a `configuration` carrying an `api_key` key now fails the execution with `EXIT_INVALID` as an unknown key (runner `run.c`, `test_run.c` scenario 14, GUI `runnerWorker.cpp`, `test_api_key` suite, docs in `README.md` and `docs/runner_contract.md` decision 4). Both plan docs were deleted once the work was done.
- Records of the past `execution.prompt` removal cleaned up (`8a1544e`): the two plan docs (`docs/plans/drop-execution-prompt.md`, `docs/plans/drop-execution-prompt-column.md`) and the one-off migration script (`acta_gui/db/drop_execution_prompt.sh`) are deleted — all repo DB files were already migrated — and the historical references are scrubbed from `docs/status.md`, `docs/cli_spec.md` (schema version note now simply "field is 4"), `docs/DBDesign.md` (the "no prompt column" bullet is a present-tense contract statement), `docs/runner_contract.md`, `acta_cli/src/tools.c` header comment, and the `tools` test comment. The present-tense contract is unchanged: no `prompt` input or output, `user = context.content` always, the resolved prompt lives only in the `prompt_resolved` log event.
- Download ergonomics in `acta_cli` for the high-volume payload data (`context.content`, execution `raw_response` / `result` / `error`): **P1** the `--tools` schema now advertises `--full` on `context.list` / `exec.list` with a `success.note` documenting the light default (commit `427671a`); **P2** a stderr warning when `--fields` names a light-omitted field without `--full` (silent-null trap; exit code unchanged, `740a3c6`); **P3** global `--out <path>` (whole stdout payload to a file, fd-1 redirect in `main.c`, stderr untouched, unopenable path → exit 10) and `--raw_out <field>` for `context get` / `exec get` (one raw unescaped field, precedence over `--id_only`/`--table`/`--fields`, `d820c6b`); **P4** large-payload file input — `context create --content_file`, `exec set-raw --raw_file`, `exec complete --result_file` (raw file content, mutually exclusive with the inline flags and, for `--content_file`, with `--json`/`--stdin`/`--from_file`; conflicts/unreadable file → exit 4, `0ce3886`); **P5** `--stream` on every `list` action — NDJSON, one JSON object per line, internal paging until the filter is exhausted (`--offset` start, `--limit` cap), mutually exclusive with `--count`/`--table`/`--id_only` → exit 4 (`a830bf2`). All registered in the `--tools` schema, `--help`, and per-action help; covered by the `tools` test suite; wire contract in `docs/cli_spec.md`.
- Raw-SQL `db exec` action removed from `acta_cli` (design change, `docs/plans/remove-db-exec.md`): `db exec` (positional / `--sql` / `--file` / `--sql_stdin`, 64 KiB cap, KI-5 SELECT guard) is replaced by `db init`, which applies only the static canonical schema embedded from `acta_db/schema.sql` (generated header `acta_db/include/schema_sql.h`, regenerated by the `acta_db` Makefile rule). Fresh file → schema applied; already schema'd file → idempotent no-op; partially applied or foreign file → fail closed, exit 4. The canonical schema moved `acta_gui/db/schema.sql` → `acta_db/schema.sql` (the GUI resource `:/db/schema.sql` points at the same file). `acta_db_exec` stays in `acta_db` as the DB-layer primitive; its only remaining caller is the GUI first-launch schema application (header comment and `docs/DBDesign.md` updated). Test pinning: the `db` suite now covers `db init` (fresh / no-op / partial / foreign / input rejection / `db exec` → unknown-action exit 10), the `tools` suite expects 75 entries with `db.init` present, `db.exec` absent, and the `--tools` schema `version` bumped 4 → 5 (compact header "v4"); runner / `acta_db` test seed paths point at `acta_db/schema.sql`. (The first `make all` failed at `sqlite3.h:188` because the generated `schema_sql.h` was missing the `;` terminating the `ACTA_SCHEMA_SQL` declaration — the unterminated initializer swallowed the translation unit up to the first code token in `<sqlite3.h>`; fixed in the `acta_db` Makefile generator rule and the generated header.) Two further `acta_db_user_tables` bugs found in the follow-up test runs are also fixed: a valid empty result was returned as NULL-on-failure (breaking fresh-file `db init`), and the buffer was never initially allocated (segfault on the first row, killing `test_db` and `test_tools`); the `db.h` contract now states the zero-length-but-valid empty result. The full `make all` / `make test` run is green.

**Known issues:** tracked in `docs/known_issues.md` — 6 items found by
read-only testing against a real DB (2026-09-18), extended from 2026-09-19
with items #7–#11 from a full static code review. #1–#4 are fixed and
regression-pinned: #1 (trailing positional emitted payload alongside rc 10,
`ce14a22`), #2 (`model_revision get-latest` ignored soft-delete, `d678a12`),
#3 (`model_revision get` on a deleted row → rc 1, `4451244`), #4 (revision
listers/counter live-only by default + `--include_deleted` / `--deleted`
machinery, `c437b3e`). All six are now fixed: #5 (`--verbose` help wording —
the global usage line reads `debug level 0-3 (repeatable, default 0)` and
every per-entity help line uses `--verbose [N]` with the same
`debug level 0-3` wording) and #6 (seed row 1 of `acta_cli/acta_test_ref.sql`
and `acta_test_ref.db` now carries `event='seed'` instead of `''`; `event` is
`TEXT NOT NULL`, so NULL was never a valid value — data-only edit). No
compile or test-shape change for either. Of the static-review items, #7
(`main.c` included Windows-only `<io.h>` for the `--out` fd-redirect with no
POSIX fallback; now `#ifdef _WIN32` selects `<io.h>` vs `<unistd.h>`, so the
CLI compiles on Linux/macOS as the README claims — build-portability only) is
fixed (`c327a49`); #8 (bearer header snprintf'd into a fixed 512-byte buffer,
silently truncating long `api_key`s in `acta_runner`; now built on a buffer
sized for the key, `malloc` failure → `BACKEND_ERR_ALLOC`) is fixed (`c64b74c`); #9
(pass-2 `cmd_args_flag` consumed a flag token as the value — `model list
--name --table` silently filtered by name "--table"; `cmd_args_flag` now
matches the positional walkers' tokenization and `cmd_args_validate`
rejects a value flag without a non-flag value as a CLI usage error, exit
10) is fixed (`cebd993`); #11 (GUI "Show trash"
persistence on the context/execution panels — the checkbox states are now
saved to QSettings in `closeEvent` and restored in the constructor, mirroring
the skill/model panels, `494e988`) and #10 (sweep `last_activity` page cap —
the total
log-row count is taken first and only the LAST page is fetched, so the
overall newest row is seen for any row count; pinned by the > 10,000-row
scenario in `acta_runner/tests/run/test_sweep.c`, `c3d37b8`) are also fixed — all 11 verified green by the full-suite run (2026-09-19). All
previously tracked issues are
fixed and regression-pinned in the `acta_db/tests`, `acta_cli/tests` and
`acta_runner/tests` suites: the three `acta_db` review
issues (light-projection listers, open-time pragma check-and-report,
`acta_db_exec` contract) and CLI known-issues KI-1..KI-8 (`--sql_stdin`
flag, unknown JSON keys, `--raw_out` on NULL field, trailing `help`
positional, `db exec` SELECT guard, `--id_only` on `list`, error-detail
propagation, `--all` no-op). The wire/error contracts they established are
documented in `docs/cli_spec.md`; fix history is in the git log. (The in-process `test_tools` cross-check failure on the `db.backup` schema entry that briefly appeared while the backup work landed is fixed — double free in the test's `cross_check` helper, commit `fd1e1a4`; the full suites are green.)

**Deliberately not part of the product (owner decisions, not missing features):** automatic retries (owner decision, 2026-09-12 — retrying without knowing the cause is pointless; the operator decides whether to retry); streaming responses (owner decision, 2026-07-10 — an execution is a single, non-interactive call, so token-by-token output has no role). A failed execution is retried manually via the `failed → pending` reset (GUI Retry button / `acta_cli exec reset <id>`).

**Done — `acta_cli db backup --to <target>`** (the item formerly listed as planned): an atomic snapshot of the open database via the SQLite backup C API (`acta_db_backup` in `acta_db` — WAL state folded in, so the GUI/CLI/runner do not need to be closed), with built-in `PRAGMA quick_check` on the backup (a failed check is deleted and reported as failure), strict target validation (no quote/semicolon; backslash rejected on POSIX only, allowed on Windows where it is the native separator; self-backup and silent overwrite rejected), plus documented backup frequency (before destructive operations; at the end of any session that produced executions). Landed with the `--tools` entry `db.backup` (75-entry table), per-action help, the `backup` tests in `acta_cli/tests/db/db_test.c`, the `cli_spec.md` row + contract paragraph, and the `DBDesign.md` maintenance section. The in-process `test_tools` cross-check failure on the `db.backup` entry initially observed while landing this is fixed (double free in the test's `cross_check` helper, commit `fd1e1a4`) — the `test_tools` suite is green.

**Done — per-machine config file (resolves the former "Future constraint" item):** the optional per-machine config file (`ACTA_Gamma.conf`, `0600`-class permissions, read by all three binaries) is implemented, holding the four settings: `"api_key"` (fallback under `$OPENAI_API_KEY`), `"db"` (fallback under `--db` / `$ACTA_DB`), `"max_chars"` (per-machine override of the prompt-size limit), and `"timeout"` (fallback under `--timeout`). The shared `acta_conf` helper (`acta_conf_parse`, `acta_conf_api_key_status`, `acta_conf_read`, `acta_conf_default_path`, and the resolution helpers `acta_conf_resolve_max_chars` / `acta_conf_resolve_timeout` with the built-in defaults) lives in `acta_db` (`conf.h` / `conf.c`); the key policy is wired into the runner's `cmd_run`, the config-file `db` step into `acta_db_resolve_db_path` (both `acta_dbpath.c` copies) and the GUI's `MainWindow::defaultDbPath`, and the permission gate is hard refusal on POSIX and warning-plus-read-anyway on Windows. Full contract: `docs/runner_contract.md` (decision 4) and `docs/cli_spec.md` (DB-file resolution); user-facing summary in the README, *Environment variables and the per-machine config file*.

**Done — `max_chars` prompt-size preflight check:** `run_execution` (carried by `cmd_run` and the GUI worker) takes the `max_chars` limit, resolved file-over-builtin (`acta_conf_resolve_max_chars`, built-in default 100,000 chars), and the preflight size check fails the execution with `EXIT_INVALID` **before any backend call** when `strlen(prompt_template) + strlen(context.content)` exceeds the limit. Contract in `docs/runner_contract.md` (decision 8 + pipeline step 3), README "How a run is assembled"; tests in `acta_runner/tests/run/test_run.c` (scenarios 16/17).

**Done — `acta_runner check` token-free backend health check** (`docs/plans/runner-health-check.md`; verified: `make all`, `make test` incl. `tests/check/test_check`, and `make gui` all green): a read-only `acta_runner check <model-record-id>` (DB mode: the `models` row is read for `base_url` + `model_identifier`, no execution created/claimed/logged; unknown id → `EXIT_NOT_FOUND`) or `acta_runner check --base-url <url> --model-identifier <id>` (standalone: no DB lookup — main dispatches it before DB resolution, `db` handle NULL). Two token-free GETs — `GET /health` (200 ok; 503 `model still loading`; unreachable `server unreachable`) then `GET /v1/models` (id present → ok + catalog `max_context`; absent → `model not served`; endpoint error → `catalog unreachable`) — one JSON verdict line on stdout (`{"ok":true,...}` / `{"ok":false,...,"verdict":"..."}`), exit 0/1/4/12/13 from the existing code space; timeout resolution is the existing `--timeout` → config `"timeout"` → 600 s. The shared `/health` + catalog request/response path is factored into `backend_preflight()` in `acta_runner/src/backend.c` (the `run` preflight and `check` call the same function; the run pipeline still sends the api key on those GETs per decision 4); new flags `--base-url` / `--model-identifier` in `argparse.c`'s flag specs; `check` action + help in `main.c`. Test suite `acta_runner/tests/check/test_check.c` (in `make test`): every scenario asserts the stub's `/v1/chat/completions` request count stays 0 and no execution/execution_log rows exist; the stub gained `models_status`, `max_context`, and `stub_server_chat_requests()`. Contract in `docs/runner_contract.md` ("check action"), README quick start + end-to-end example + sweep paragraph.
